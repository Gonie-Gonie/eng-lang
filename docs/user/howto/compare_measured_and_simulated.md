# Compare Measured And Simulated TimeSeries

Status: current workflow pattern, not a broad solver claim.

Use typed schemas for both sources, align by Time, derive difference metrics,
and record validation results in review artifacts. For now, start from official
TimeSeries examples and use internal measured-vs-simulated regression examples
only when the current status document marks the scenario supported.

# Sensor QC Report

This page is the workflow-book name for the current uncertain sensor report
fixture.

Start with [Uncertain sensor report](uncertain_sensor_report.md), which covers:

- typed sensor CSV promotion
- unit-aware TimeSeries calculation
- measured uncertainty metadata
- confidence-band report output
- reviewable uncertainty metadata

Future sensor QC workflow docs should separate deterministic quality checks from
uncertainty-specific report behavior.

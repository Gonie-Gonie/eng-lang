- Batch 1085: Centralized uncertainty compatibility argument aliases, canonical replacements, and valid constructor contexts in one compiler-owned registry; routed LSP quick fixes, VS Code diagnostics, generated TextMate first-paint rules, and native IDE lexical fallback through that metadata; removed context-sensitive aliases from the broad legacy workflow-option list; made TextMate uncertainty regions preserve nested call/parenthesis context; and added actual-tokenizer regressions so equal spellings outside their supported constructors are not colored as deprecated.
- Batch 1084: Canonicalized uncertainty constructor arguments across compiler, LSP, VS Code, examples, and docs; retained executable compatibility names with exact-key warnings and quick fixes; removed alias completion noise; and added deprecated first-paint plus semantic coloring.
- Batch 1083: Made `sample grid`, `sample random`, and `sample lhs` the only advertised sampling methods while retaining executable `sample uniform`, `sample latin_hypercube`, and `sample latin-hypercube` compatibility; added exact method-token compiler warnings, range-only LSP quick fixes, canonical method-context completion in Rust and VS Code fallback, context-specific deprecated semantic/TextMate coloring, generated metadata guards, diagnostic links/tags, and canonical public/internal documentation without deprecating `uniform(...)` distributions.
- Batch 1082: Made `mlp(...)` the only neural-network constructor advertised by completion while retaining executable `ann(...)` source compatibility; added an exact `W-ML-ANN-ALIAS` compiler range, LSP/VS Code deprecated diagnostics, a range-only `ann` to `mlp` quick fix, deprecated first-paint and checked-code coloring, generated metadata guards, and canonical public/internal documentation.
- Batch 1081: Repaired the VS Code TextMate audit so current ASAR-based VS Code installs are discovered instead of silently skipped; generated one compiler-owned semantic snapshot bundle for examples and grammar fixtures; made modifier-specific fallback scopes mandatory instead of accepting a generic token-type match; cross-checked 2,083 role-bearing semantic tokens against first-paint scopes across 127 examples; repaired report/test/solver blocks, `connect ... to ...`, standalone `with {`, call-style `predict(...)`, DB `.table(...)`, policy constants, and `apply ... over` operands; removed the dead operator modifier scope; and added LSP, grammar, theme, and example-wide regression coverage.
- Batch 1080: Replaced unavailable numeric fallbacks on native model, model-card, metric, and prediction ReviewDocument rows with typed runtime results carrying computed coefficients, metrics, train/test counts, model/training/prediction hashes, prediction schema/outputs/case IDs, and distinct provenance; added model/prediction evidence counts and Runtime Review summaries; documented the discriminated JSON Schema contract; surfaced normalized rows in the native IDE Model Review panel; and added Rust plus three-workflow regression gates.
- Batch 1079: Normalized generated-file and native SQLite write side effects into ReviewDocument `runtime_result` rows; resolved `standard_text` targets from their accepted `with.output` option and exposed structured DB table targets in static review; added artifact/database/manifest hashes, transaction/schema status, table and row evidence, Runtime Review rows/counts, public schema definitions, focused Rust regressions, and a six-row Workflow 02 gate.
- Batch 1078: Completed normalized runtime projection for materialized tables, TimeSeries, explicit coverage checks, and source-derived time axes across matching units/quantity, symbol, derived-value, and calculation rows; added TimeSeries/coverage counts, coverage sample summaries, and source table/column/axis evidence to Runtime Review HTML; and added focused Rust plus Workflow 03 gates for the previously empty core rows.
- Batch 1077: Added a validated `eng_report` API for full or bare ReviewDocument input; moved runtime HTML generation after final review enrichment so `report.html` and saved `review.json` share one semantic fingerprint; rendered core runtime values, source/hash evidence, status, and full validation expressions from normalized rows instead of parallel ReportSpec validation data; rejected incomplete review input; and added report/runtime tests plus artifact and three-workflow fingerprint gates.
- Batch 1076: Projected resolved Args, schema/table evidence, scalar and TimeSeries values, calculation/output results, table-transform counts, and validation outcomes into normalized ReviewDocument `runtime_result` rows; added runtime evidence/status plus deterministic changed-section rehashing while preserving unchanged static hashes; closed missing `symbols` and `config_promotions` hash coverage; preserved full command-validation clause expressions instead of truncating `between` rules; exposed runtime columns in the native IDE and source-hash-matched VS Code Review panel; and aligned artifact, CLI, status, maturity, workflow, tutorial, and CI documentation.
- Batch 1075: Moved ReviewDocument extraction, validation, and semantic diff into one compiler-owned Rust API; routed both CLI diff forms and a new bounded Native IDE Tauri command through it; rejected malformed review JSON instead of silently treating it as changed; added a Review-tab baseline picker with automatic post-run refresh, section hashes, item-level added/removed/changed rows, and advanced payload inspection; and closed the stale native IDE diff-panel documentation gap.
- Batch 1074: Exposed the existing native ReviewDocument comparison engine as `eng review diff <old-review.json> <new-review.json>` with concise and JSON stdout modes plus optional `semantic_diff.json` output; reused the same reader, diff payload, and artifact writer as `eng review --against`; fixed section comparison to use the union of old/new section hashes so removed sections and their items are reported; added parser, removal, equivalence, and built-binary smoke coverage; and reconciled CLI, reviewability, status, maturity, artifact, and CI documentation that still called the standalone command planned.
- Batch 1073: Removed planned and internal stdlib modules from ordinary LSP, VS Code fallback, and native IDE completion while preserving explicit-import diagnostics and semantic coloring; centralized the public API status boundary in the module registry; added Rust and generated-metadata guards proving public modules remain discoverable while `eng.building`, `eng.system`, and `eng.ml` are not advertised as usable APIs; and documented the distinction between completion availability and explicit-source recognition.
- Batch 1072: Removed duplicated status wording from the user-facing `eng.case`, `eng.db`, `eng.template`, and `eng.ml` completion descriptions; made module purposes start directly with the capability instead of producing labels such as `Native: Native ...` or `Internal: Internal ...`; and added a registry validation/test that rejects `Supported`, `Native`, `Planned`, or `Internal` purpose prefixes when the module status already supplies that label.
- Batch 1071: Unified explicit computed-scalar declarations with the native call path: explicit `duration_above` and `rmse` expressions now receive the same compiler-owned validation and exact ranges as inferred bindings; explicit statistic, RMSE, and integration expressions remain available to runtime materialization; member-path integration sources record runtime-resolved metadata; and `typed_payload.numeric_values[].value` now converts native statistic, integration, and metric results from their kernel units into each declaration's display unit before print/export reuse.
- Batch 1070: Reconciled the globally completed and colored `duration_above` and `sum` APIs with native behavior: added compiler-owned structured statistic-call parsing; made `duration_above(series, threshold)` a typed `Duration [s]` value call with exact source/threshold/unit diagnostics; implemented sample-sum execution in runtime, uncertainty propagation, and JIT reduction; reused one runtime statistic kernel for report and print/export evaluation; materialized computed statistic bindings into `typed_payload.numeric_values[].value`; made explicit computed bindings printable through that payload; and aligned completion wording, semantic coloring tests, module symbols, CLI/language/TimeSeries/status/editor documentation, and generated metadata.
- Batch 1069: Reconciled the public `rmse(left, right)` API with native execution: registered `rmse` as a compiler built-in; made the compatibility `rmse left vs right` command parse and lower to the same canonical call; added shared compiler-owned operand parsing used directly by runtime metric materialization; preserved quantity-aware result typing for known TimeSeries inputs and native display-unit conversion; added exact `E-RMSE-CALL-001` ranges; switched the measured-vs-simulated fixture to the preferred call; classified RMSE as a validation builtin for TextMate first-paint while applying report/timeseries/validation semantic roles to both call operands; expanded grammar coverage to 1000 tokens; and updated the eng.quality registry, completion wording, CLI/language/TimeSeries/status documentation, compiler/runtime/LSP regressions, and generated editor metadata.
- Batch 1068: Reconciled the public external-value helper catalog with compiler and runtime behavior: made `url(...)` and non-secret `env(...)` real top-level and Args-default built-ins instead of editor-only colors/completions; added typed `Url`/`String` inference, normalized Args values, native print/write value evaluation, environment fallback and explicit missing-value failure, provenance statuses, and run-lock invalidation from environment/existence observations; added precise constructor diagnostics and LSP ranges for bindings and Args defaults; clarified completion wording and the `env` versus `secret env` boundary; and added compiler/runtime/LSP regressions including ordinary whitespace before call parentheses.
- Batch 1067: Separated parser-lowered command expressions from user-written function calls in the compiler AST; kept bound `render template ...` commands typed as `TemplateFile`; removed `render` from the callable built-in set; added `E-RENDER-CALL-001` with command-syntax recovery guidance and an exact compiler-owned function-name range; propagated the diagnostic unchanged through LSP UTF-16 ranges; made Native IDE embedded-source smoke contracts normalize Windows CRLF before multiline checks; and documented the command-only public API across current status, language, CLI, workflow-module, and VS Code references.
- Batch 1066: Replaced four divergent percentile recognizers with one compiler-owned `parse_percentile_fraction` contract shared by semantic analysis, runtime materialization, uncertainty propagation, and JIT planning; bounded built-ins to integer `p1` through `p100` while retaining leading-zero forms such as `p05`; exported the same bounded pattern through editor metadata; added dynamic TimeSeries built-in coloring for LSP, native IDE first-paint, call-style TextMate, and bare `summarize ... by [...]` statistics; kept `p0`, `p101`, decimal forms, and unrelated `p`-prefixed calls out of built-in coloring; and expanded first-paint grammar fixtures from 993 to 999 token expectations.
- Batch 1065: Promoted the compiler's nine dimensionless scalar math functions into one public catalog consumed directly by LSP semantic scanning, completion, generated editor metadata, and TextMate grammar generation; changed `sqrt`, `exp`, `ln`, trigonometric, and inverse-trigonometric calls from generic user-function coloring to built-in `defaultLibrary` coloring; added missing `predictor(...)` completion and solver coloring; made call-style `predict(...)` use the same solver role while preserving command-style model prediction; and expanded LSP plus first-paint grammar fixtures from 982 to 993 token expectations.
- Batch 1064: Made shared top-level expression splitting quote/escape-aware across function and object calls, component/behavior arguments, process `env` objects, sampling ranges, and scalar dimension helpers; kept commas, semicolons, parentheses, and arithmetic symbols inside quoted values inert; preserved source-slice argument ranges; removed false process-environment diagnostics for quoted delimiters; and added parser, compiler, and LSP regressions covering quoted commas, parentheses, escaped quotes, and workflow options.
- Batch 1063: Extended recursive user-function diagnostics from direct call arguments into parenthesized scalar arithmetic operands and built-in-call arguments; mapped every traversed operand through compiler-owned source spans; kept the generic unresolved-argument diagnostic only when no nested call explains the failure; made the traversal quote/escape-aware so call-like string contents remain inert; aligned dimensionless math helpers with built-in classification; limited percentile built-ins to `p` plus digits so arbitrary `p`-prefixed calls report as unknown; added compiler source-slice and CRLF LSP range regressions; and corrected public incremental API comments that still described valid nested calls as unsupported.
- Batch 1062: Made nested user-function diagnostics recurse through direct scalar-call arguments; mapped each child call through its compiler-owned source span; changed inner dimension mismatches to underline the failing inner argument and unknown inner calls to underline the inner function name; reused the known inner return dimension for the outer contract; suppressed duplicate outer `E-FN-CALL-003` diagnostics after an inner failure; preserved built-in math-call handling; and added compiler source-slice plus CRLF LSP range regressions.
- Batch 1061: Extended registered pure scalar calls to recursively nested call arguments in full semantic inference and bounded imported-scalar suffix rechecks; reused the same structured token resolver for direct, arithmetic, explicit, `const`, and imported-constant forms; accumulated and validated every nested call contract under declaration-signature trust for fresh analysis and final `unit_consistent` trust for report reuse; prevented invalid or unknown nested calls from being accepted through legacy unit-only fallback; improved `E-FN-CALL-003` guidance for scalar call arguments; and proved compiler report/review/bytecode plus persistent-LSP equality through nested-call edits, clearing, restart, and import invalidation while retaining fallback for invalid, non-scalar, workflow, and richer expressions.
- Batch 1060: Extended scalar function-call support from direct RHS values to one or more atomic call operands inside pure `+`, `-`, `*`, and `/` arithmetic across fast, explicit, and `const` declarations plus unchanged imported constants; reused the structured component scalar tokenizer/parser for precedence, parenthesis, unit, malformed-operator, arity, and argument-dimension validation; separated declaration-signature trust during full semantic inference from final `unit_consistent` trust during incremental report reuse; fixed direct-call parsing so `f(a) + g(b)` is not misread as one call; preserved inferred type/hover/unit metadata for valid call arithmetic; proved compiler report/review/bytecode and persistent-LSP equality through repeated edits and import invalidation; and retained full fallback for nested calls, mismatched dimensions, unsupported functions, diagnostics, and richer constructs.
- Batch 1059: Completed direct imported scalar-function suffix reuse across all three scalar declaration forms by extending the strict annotated-expression contract to root `const` declarations; required exact arity, dimension-valid scalar arguments, registered unit-consistent function contracts, and return dimensions matching explicit or `const` annotations; admitted unchanged imported scalar constants that directly call an eligible imported function; proved compiler report/review/bytecode and persistent-LSP equality against fresh checks through repeated edits and import invalidation; and retained full fallback for wrong arity or return dimensions, nested calls, call arithmetic, built-ins, diagnostics, and richer constructs.
- Batch 1058: Extended direct imported scalar-function suffix reuse from fast bindings to registered explicit scalar declarations; factored one strict function-call contract for exact arity, dimension-valid scalar arguments, registered parameter/return quantities, and unit-consistent functions; added explicit return-dimension matching against the declared quantity both during suffix analysis and prior-report validation; proved explicit-only and mixed persistent-LSP reuse against fresh compiler snapshots; and retained full fallback for mismatched returns, invalid arguments, `const` calls, nested calls, call arithmetic, diagnostics, and richer constructs.
- Batch 1057: Extended bounded scalar suffix rechecks across unchanged static imports that contribute pure registered scalar functions; exhaustively validated imported function ownership, source spans, unique names, registered parameter/return dimensions and canonical units, and local-record shape; allowed a fast-binding RHS to directly call one of those functions with exact arity and dimension-compatible scalar arguments; preserved imported constants and function contracts across root edits, clearing, restart, persistent-LSP cache reuse, and recursive import invalidation; proved compiler report/review/bytecode and LSP snapshot equality against fresh checks; and retained full fallback for non-scalar functions, edited imports, invalid arguments, annotated or `const` calls, nested calls, call arithmetic, diagnostics, and richer constructs.
- Batch 1056: Closed a full-analysis IDE metadata gap for pure scalar arithmetic over typed aliases; reused the compiler's structured component-parameter arithmetic parser to evaluate result dimensions, disambiguated registered quantities from declaration names such as `dT` and `ratio`, preserved an operand type when a result dimension remained ambiguous, and now emits inferred declarations, typed bindings, hover hints, type information, and unit derivations for alias-only arithmetic without requiring a unit literal. Applied the same inference to top-level, scoped `where`, and class-field expressions; strengthened static-import compiler and persistent-LSP fixtures so imported const arithmetic retains editor hover metadata through suffix reuse and recursive import invalidation; and limited the structured path to resolved registered scalar operands and representable result dimensions without broadening general incremental-analysis eligibility.
- Batch 1055: Extended bounded scalar declaration suffix rechecks across unchanged static file imports when every recursively imported semantic definition is a pure registered scalar `const`; separated imported constants from root declaration-mode counts, validated unique source ownership plus source-aware expected/inferred/constant/typed records, preserved imported environments across root edits, clearing, restart, and all three public APIs, proved compiler report/review/bytecode and persistent-LSP snapshot equality against fresh checks, invalidated recursive dependents when an imported open buffer changed, and retained full fallback for import edits, functions or other imported definitions, diagnostics, calls, caches, and richer constructs.
- Batch 1054: Extended scalar declaration suffix rechecks across unchanged supported `use/import eng.*` module prefixes; reparsed and validated the preserved prefix, matched its exact import semantic records and syntax/token/declaration counts against the prior report, retained imports while changing, clearing, and restarting the scalar suffix, proved compiler report/review/bytecode and persistent-LSP snapshot equality against fresh checks, and kept full fallback for import-line edits, supported imports inside the affected suffix, planned/internal/unknown modules, file imports, diagnostics, calls, caches, and richer constructs.
- Batch 1053: Expanded the unified scalar declaration suffix engine to pure top-level `const` declarations with registered scalar quantity types; allowed constants to interleave with fast and explicit declarations and participate in backward aliases and pure scalar arithmetic; patched `ConstInfo`, expected, typed, hover, type, unit, syntax-count, and workflow records together across value/type/unit/name edits, additions, removals, complete clearing, and trivia-only restart; kept the homogeneous compatibility APIs const-free; proved compiler report/review/bytecode and persistent-LSP snapshot equality against fresh checks; and retained full fallback for calls, non-scalar constants, forward/unresolved references, duplicates, diagnostics, imports, caches, and richer constructs.
- Batch 1052: Made compatibility-only `regression_table(...)` and `train_regression(...)` visibly non-preferred across compiler diagnostics, exact call-name ranges, model-plus-deprecated semantic tokens, TextMate first-paint scopes, VS Code diagnostic tags, and documentation; added a bounded single-line migration action to `train regression <table>` plus canonical attached options; corrected alias-specific ML error wording; clarified that native linear regression computes real model metrics and artifacts without Python; and reconciled stale backlog wording that still listed bounded parallel case scheduling as unimplemented.
- Batch 1051: Consolidated the three scalar incremental compiler APIs onto one mode-driven suffix engine; reduced the fast-only and explicit-only entry points to compatibility wrappers that enforce their original declaration-kind policy; removed duplicate raw-line boundary detection, suffix parsers, report-shape validators, semantic analyzers, patch logic, and temporary compatibility aliases; added trivia-only cross-style rejection coverage; and retained all homogeneous, mixed, strict-fallback, fresh-equivalence, and persistent-LSP cache behavior while removing more code than the cleanup added.
- Batch 1050: Added the unified public `recheck_scalar_declaration_suffix_incrementally` compiler API for clean top-level scalar documents; allowed fast bindings and registered explicit declarations to interleave and switch style inside the affected suffix; tracked independent inferred, expected, and shared typed/hover/type/unit prefix indexes while patching syntax counts and the workflow line atomically; moved the persistent LSP cache from two homogeneous probes to the unified path; proved style transitions, suffix growth, complete clearing, trivia-only restart, compiler report/review/bytecode equality, and LSP snapshot equality against fresh checks; retained the existing homogeneous APIs as compatibility boundaries; and kept full fallback for unresolved/forward references, duplicate names, incompatible dimensions, calls, diagnostics, imports, caches, token-bearing non-declaration lines, and richer constructs.
- Batch 1049: Added a compiler-owned incremental path for declaration-only scalar documents with registered explicit quantity annotations and pure scalar RHS expressions; preserved the prefix before changed trivia/declarations; atomically patched expected-type, typed-binding, hover, type-info, unit-derivation, syntax-count, and workflow-line records across type/unit/RHS edits, declaration addition, complete clearing, and trivia-only restart; integrated the path into the persistent LSP cache; proved report/review/bytecode and LSP snapshot equivalence with fresh checks; and retained full fallback for mixed fast/explicit declarations, unresolved/forward references, calls, non-scalar types, diagnostics, imports, caches, and richer constructs.
- Batch 1048: Expanded strict scalar suffix reanalysis from literals and backward aliases to compiler-validated pure `+`, `-`, `*`, and `/` arithmetic over registered-unit literals, parentheses, and earlier typed scalar bindings; reused the component parameter arithmetic grammar to reject malformed, unresolved, dimensionally incompatible, call, and workflow forms; proved input type propagation through arithmetic and aliases plus compiler report/review/bytecode and persistent-LSP snapshot equivalence with fresh checks; and retained full fallback for diagnostics, imports, caches, token-bearing non-binding lines, and richer language constructs.
- Batch 1047: Expanded scalar suffix reanalysis across binding cardinality changes; allowed middle/final binding insertion, multi-binding removal, complete clearing, and restart from a trivia-only report; atomically patched inferred/type/hover/unit vectors, token/AST/binding/line counts, and the first top-level workflow line; proved compiler report/review/bytecode and persistent-LSP snapshot equivalence with fresh checks; and retained fallback for forward/unresolved aliases, duplicate names, compound expressions, token-bearing non-binding lines, diagnostics, imports, caches, and richer language constructs.
- Batch 1046: Replaced same-line-count and same-line-ending scalar suffix matching with a byte-exact raw-line common-prefix boundary; reparsed old/new scalar suffixes independently so standalone trivia insertion/removal and LF/CRLF shifts update absolute spans, line numbers, and syntax line counts without full analysis; proved chained compiler report/review/bytecode and persistent-LSP snapshot equivalence with fresh checks; and retained fallback for changed binding cardinality, token-bearing non-binding lines, unresolved/compound expressions, diagnostics, imports, caches, and richer language constructs.
- Batch 1045: Expanded scalar suffix reanalysis across variable-width token-free trivia edits; preserved records before the first binding at or after the first changed standalone comment or blank line; reparsed absolute spans and reanalyzed the affected numeric/backward-alias suffix once; proved compiler report/review/bytecode and persistent-LSP snapshot equivalence with fresh checks; and retained full fallback for binding layout changes, token-bearing non-binding lines, compound expressions, diagnostics, imports, caches, line-count/ending changes, and richer language constructs.
- Batch 1044: Expanded scalar binding suffix reanalysis from exactly one changed line to coordinated multi-line binding edits; reparsed and reanalyzed once from the first changed binding; allowed declaration and alias renames when line identity, global name uniqueness, and backward source-order resolution remain valid; proved atomic rename/type propagation equals fresh compiler review/bytecode and LSP snapshots; and retained full fallback for changed trivia lines, incomplete or duplicate renames, unresolved aliases, compound expressions, diagnostics, imports, caches, line-count/ending changes, and richer language constructs.
- Batch 1043: Added a prefix-aware semantic analysis boundary for token-changing scalar binding suffixes; expanded the compiler/LSP fast path from independent numeric literals to backward alias chains; propagated changed source units and alias targets through later inferred/type/hover/unit records while preserving the unchanged prefix; renamed the public compiler/LSP path around its scalar-suffix contract; proved report/review/bytecode/LSP equivalence with fresh checks; and retained full fallback for unresolved aliases, compound expressions, multi-line edits, diagnostics, imports, caches, line-count/ending changes, and richer language constructs.
- Batch 1042: Expanded the token-changing compiler fast path from the final line to any one independent numeric binding in a clean binding-only document; preserved the unchanged report prefix; reparsed and semantically reanalyzed the changed line plus every later binding whose absolute spans shifted; validated unchanged line endings, stable names, unique numeric records, and isolated old suffix ownership; proved variable-width non-terminal edits and trailing spans equal a fresh full report/LSP snapshot; and retained full fallback for multi-line, renamed, dependent, diagnostic, import, cache, richer-language, line-count, and line-ending changes.
- Batch 1041: Added the first token-changing within-document compiler fast path for clean top-level numeric-binding documents: reparsed and semantically reanalyzed only the changed final logical line with absolute source spans, verified the isolated old result owned the final report records, patched the inferred/type/hover/unit records and syntax token count, rebuilt the editor snapshot lazily, and proved report/review/bytecode/LSP equivalence with fresh full checks while earlier-line, renamed, dependent, diagnostic, import, cache, and richer-language edits retain full-analysis fallback.
- Batch 1040: Added a compiler-owned report retarget path for changed open documents whose complete token stream, absolute token spans, and token-bearing source lines remain identical; reused prior parse/semantic results for safe comment-only and blank-line edits; rebuilt source-dependent editor snapshots lazily; retained hard invalidation for recursive import dependents; and proved cache-bearing, semantic-token, anchored-line, and source-position changes use fresh full analysis without claiming general incremental parsing.
- Batch 1039: Added an executable Light/Dark/native IDE screenshot comparator for supplied full-window captures; enforced baseline dimensions, significant per-channel RGB deltas, changed-pixel and mean-channel thresholds; emitted machine-readable summaries plus magenta diff PNGs; integrated a zero-diff decoder self-check into `editor-visual-check`; verified a fully altered capture fails; and retained manual capture/state review instead of claiming cross-platform pixel equivalence or automated GUI control.
- Batch 1038: Added a shared exact-source compiler-analysis cache to each persistent LSP document; made diagnostics, completion, hover, semantic tokens, Outline, folding, navigation, references, rename, and the extension snapshot request reuse one `CheckReport` plus a lazy `LspSnapshot`; retained cached analysis across same-text saves/version changes; invalidated changed documents and recursive open import dependents while preserving unrelated document caches; cleared all open analyses for watched-file changes; and added hit/miss/lazy-projection regressions without claiming within-document incremental parse or semantic recomputation.
- Batch 1037: Replaced the native case runner's hardcoded sequential metadata with an explicit validated `scheduler = sequential|parallel` and bounded `workers` contract; executed case result expressions on real scoped worker threads with deterministic balanced static partitions and source-order results; preserved dependent output evaluation per case; added configured/effective worker counts, worker slots, lifecycle events, scheduler-aware cache identity, unified `native_expression_runner` wording, artifact/schema/IDE/LSP/VS Code exposure, actual-concurrency and uneven-partition regressions, and Workflow 02 end-to-end evidence with 8 native cases on 4 workers and zero external processes.
- Batch 1036: Replaced full-text persistent editor changes with LSP Incremental sync; made the Rust server apply ordered UTF-16 range edits transactionally while rejecting malformed line, reversed, and surrogate-splitting ranges; made the VS Code client send one minimal Unicode-safe diff with full-text fallback for older servers and CRLF-boundary changes; added unit, stdio, client, and package-contract coverage; and kept compiler parse/semantic recomputation explicitly separate instead of claiming full incremental analysis.
- Batch 1035: Re-audited the completed AST, semantic-symbol, diagnostic, semantic-token, and Outline source-range contracts; added an exhaustive public `AstItem::primary_span()` accessor with explicit anchor-versus-full-extent wording; added a 153-source parser corpus gate for non-empty UTF-8 ranges, exact byte line/column metadata, and root/import ownership; and reconciled the two stale parent checklist items without changing their already-complete child claims.
- Batch 1034: Added bounded, byte-identical VS Code and native IDE visual-acceptance workspaces; removed a planned `eng.stats` dependency exposed by the real native check; captured and inspected EngLang Light, EngLang Dark, and native IDE full-window baselines with clean language/diagnostic state; added screenshot manifest integrity plus semantic/diagnostic fixture coverage to `editor-visual-check` and `lsp-check`; and documented the intentional manual pixel-review boundary.
- Batch 1033: Replaced per-provider VS Code `eng-lsp` subprocesses with one dependency-free persistent stdio JSON-RPC client; synchronized open/change/save/close buffers and watched files; consumed versioned publishDiagnostics; routed hover, completion, navigation, symbols, folding, formatting, quick fixes, and protocol semantic tokens through the shared process; retained bounded CLI endpoints only as compatibility fallbacks; added an in-session `englang/snapshot` request for review decorations; and covered framing, UTF-8 byte lengths, cancellation, crash restart, direct semantic-token use, and graceful shutdown.
- Batch 1032: Retained a compiler-owned root/import source-file registry behind every source ID; exposed path lookup without serializing unsafe numeric IDs; projected portable import paths into validation spans and hover payloads; and made the VS Code Review panel distinguish result versus rule locations and open imported class rules in their owning `.eng` file.
- Batch 1031: Extended compiler source ownership from Outline into current-document diagnostics, semantic declaration overlays, structural token classification, review-risk assembly overlays, and hover fallback; made root hover metadata win same-name import collisions while retaining imported symbols for completion/reference/definition and imported-reference hover; labeled hover origin explicitly; preserved root object versus imported class-rule ownership in review validation spans; and added adversarial same-line/name import coverage with CRLF and non-BMP root text.
- Batch 1030: Added source ownership IDs to lexer/parser spans so current-document Outline cannot apply imported-file ranges to the root buffer; exposed exact table-transform and network-request binding spans plus args/test/golden/expectation container spans; moved every active Outline path to compiler-owned selection start/end coordinates; removed same-line symbol-name search and display-name-length selection inference; excluded import-owned definitions from the current document Outline; and added CRLF, non-BMP, repeated-text, synthetic-name, import, and full source-corpus range coverage.
- Batch 1029: Anchored the remaining derivative-duplicate, legacy row-selection, table-join, run-case, deprecated root, test/assert/golden, and implicit TimeSeries-fill diagnostics to compiler-owned source ranges; exposed the legacy struct name span; removed the duplicate missing-results error from `apply run_case`; added exact source-slice and non-BMP UTF-16 coverage; and tightened the 153-file diagnostic audit from 15 inferred ranges to zero.
- Batch 1028: Preserved exact function-name and argument byte ranges while parsing calls; anchored quantity ambiguity, HeatRate sum, dimensionless addition/subtraction, and function-call diagnostics to the unit, `sum`, operator, function name, or failing argument; stopped strings and function arguments from being misread as a binding's returned quantity; clarified that compatibility highlighting does not make context-only helpers public top-level calls; added CRLF, non-BMP, repeated-text, UTF-16, and fixture-corpus coverage; and reduced the 153-file diagnostic audit from 31 to 15 inferred ranges.
- Batch 1027: Replaced side-effect and database target string reconstruction with lexer-token metadata for print/log interpolation, CSV export fields, writes, DB reads/writes, nested file operations, and process runs; exposed exact keyword, operand, format, path, connection, table, and interpolation spans through the public semantic model; made native runtime DB reads/writes consume the same structured targets instead of reparsing expression text; anchored 18 process, DB, export, write, print, and file diagnostics plus their semantic roles and Outline selections to those ranges; added CRLF, non-BMP, repeated-name, UTF-16, fixture, and overlap coverage; and reduced the 153-file diagnostic audit from 49 to 31 inferred ranges.
- Batch 1026: Replaced ad hoc `promote csv/json/toml` and `promote json records` string parsing with lexer-token `PromotionDecl` metadata that accepts ordinary whitespace variation and preserves exact binding, expression, source, source-path segment, `as`, and schema spans; exposed promotion spans through `CsvPromotion`/`ConfigPromotion`; anchored schema lookup, CSV/JSON source, missing-column, Args-source, and config validation diagnostics to the schema or source operand; suppressed missing-column/field cascades when the external source could not be read; moved promotion semantic roles and Outline selection to those ranges; added CRLF, non-BMP, repeated-name, UTF-16, fixture, and corpus coverage; and reduced the 153-file diagnostic audit from 70 to 49 inferred ranges.
- Batch 1025: Preserved domain generic kind/name, domain variable type/unit, conservation, component parameter type/unit/default, component-local expression/equation, constructor argument, connect endpoint, and equation-side spans through public semantic metadata; anchored domain contracts, connect compatibility, component parameter/equation/boundary/behavior, and physical-equation diagnostics to the failing declaration, endpoint, call, argument, unit, or equation side; added domain/component quantity/unit and connection endpoint colors plus source-owned Outline selections while removing synthesized residual children; added CRLF, non-BMP, UTF-16, fixture, and corpus coverage; and reduced the 153-file diagnostic audit from 98 to 70 inferred ranges.
- Batch 1024: Preserved class field-default, validation-expression, method return type/unit/expression, and object-field expression spans through public semantic metadata; anchored every `E-CLASS-*` diagnostic to the offending class/object/call occurrence; colored method return quantities and units from those spans; moved class, validation, method, object, and explicit object-field Outline selections to compiler-owned ranges while hiding copied fields that point into the source object; added CRLF, non-BMP, repeated-text, UTF-16, overlap, and corpus coverage; and reduced the 153-file diagnostic audit from 106 to 98 inferred ranges.
- Batch 1023: Anchored every simulation and algebraic/component-solver contract diagnostic to compiler-owned source: unknown targets select the target name, missing required options select the owning `simulate`/`solve` RHS, and malformed supplied options select their exact value. Added CRLF, non-BMP, repeated-text, UTF-16, quick-fix, and corpus coverage; made option repairs prefer the Problems range and insert a missing single option into an attached or new `with` block; and reduced the 153-file diagnostic audit from 125 to 106 inferred ranges.
- Batch 1022: Preserved exact command expression, target, clause key/value, and assertion operand/operator/tolerance spans through public semantic metadata; anchored direct uncertainty comparison, percentile threshold, probability-expression, and generic validation-unit diagnostics to the offending expression; made command semantic colors plus command/assert Outline selections source-owned; made the direct-comparison quick fix honor the diagnostic range before legacy text search; added CRLF, non-BMP, repeated-text, trailing-comment, UTF-16, overlap, and corpus coverage; and reduced the 153-file diagnostic audit from 129 to 125 inferred ranges.
- Batch 1021: Preserved exact sampling binding, RHS, distribution key, and distribution value spans through public semantic metadata; anchored missing `count` and missing `uniform(lower, upper)` diagnostics to the owning `sample <method>` expression; moved sampling declaration/distribution semantic tokens and Outline selections off same-line search; added CRLF, non-BMP, repeated-text, trailing-comment, overlap, and corpus coverage; and reduced the 153-file diagnostic audit from 139 to 129 inferred ranges.
- Batch 1020: Parsed uncertainty constructor positional and named arguments into exact compiler-owned binding, expression, source, key, and value spans; moved every `E-UNC-SOURCE-*` and `E-UNC-ARGS-*` diagnostic plus uncertainty source/option semantic token to those ranges; preserved nested calls, CRLF, non-BMP text, dotted values, repeated names, and trailing-comment boundaries; and reduced the 153-file diagnostic audit from 152 to 139 inferred ranges without claiming the separate uncertainty-validation expression diagnostics.
- Batch 1019: Unified inline and attached `with` ML options as structured compiler metadata with exact expression, key, value, and per-feature spans; moved every `E-ML-SOURCE-*` and `E-ML-ARGS-*` diagnostic plus ML option semantic token to those ranges; excluded trailing `#`/`//` comments from multiline option values and ignored braces inside inline-option strings/comments; and reduced the 153-file diagnostic audit from 167 to 152 inferred ranges with a permanent non-regression gate.
- Batch 1018: Removed nine false `E-NET-INVALID-URL` diagnostics from editor grammar fixtures by resolving declared `Url`/const/local aliases before validation and leaving unsupplied runtime `args.*` values unresolved instead of malformed; added exact request/download URL operand spans through AST, semantic network metadata, diagnostics, and normalized review boundaries; ended fast-binding and download parsing at the final lexer token so trailing `#`/`//` comments cannot enter expressions or output paths; and added CRLF, non-BMP, repeated-text, alias, dynamic-Args, review, and fixture regression coverage.
- Batch 1017: Audited 403 diagnostics across 153 example, diagnostic, and grammar-fixture files; replaced 136 inferred LSP ranges with exact compiler-owned anchors for component assembly balance/loop, unconnected-port, and invalid port-domain diagnostics; fixed port declarations so trailing `#`/`//` comments cannot become part of the domain reference; preserved the assembly anchor through review/report artifacts; renamed executable assembly/equation metadata away from seed-only wording; and added CRLF, non-BMP, repeated-text, schema, and corpus regression coverage.
- Batch 1016: Split lexical string tokens around nested interpolation parameters/properties, format precision numbers, display units, and quoted import namespaces; preserved every inner semantic role at its UTF-16 range while emitting string tokens only for uncovered fragments; added non-BMP interpolation and quoted-import regression coverage; and upgraded the 133-snapshot coverage gate to reject every overlapping semantic-token pair without a string exception.
- Batch 1015: Removed the remaining 46 non-string semantic-token overlaps across import namespaces, ML operands/features, and `apply` callables; kept import targets as one atomic namespace while splitting dotted workflow paths into parameter/receiver/property/function segments; merged review-risk modifiers only at the first identifier's existing symbol range; added VS Code fallback scopes for the newly observed role pairs; and upgraded the 133-snapshot coverage gate to reject every overlapping non-string token pair.
- Batch 1014: Audited all 133 example and grammar-fixture semantic snapshots for partial token overlaps; stopped numeric fallback from splitting identifiers and literals such as `p95`, `rk4`, `expected_sha256`, and `case_001`; emitted decimal exponents as one number token; suppressed operators inside hyphenated workflow literals and compiler-owned composite units; reduced partial overlaps from 295 to 258 and non-string number/operator overlaps from 37 to zero; and added that zero-overlap invariant to the VS Code coverage gate.
- Batch 1013: Preserved complete fast-binding RHS spans plus exact ML binding/source/prediction-input spans; used those compiler ranges for inferred soft-keyword aliases, model workflow operands, and ML source diagnostics; collapsed `model`/`records` keyword-variable overlaps into one role-rich variable token; and kept `promote json records` plus dotted member occurrences independently classified.
- Batch 1012: Preserved exact `write` source-expression spans through `WriteDecl` and public `WriteInfo`; made simple identifier sources replace lexical keyword fallback only at that compiler-owned range; merged binding, workflow, side-effect, and DB modifiers into one token; kept real uses such as `promote json records` keyword-colored; and added CRLF plus non-BMP regression coverage.
- Batch 1011: Preserved parser-owned `where`/`with` block anchors and `where` local spans in semantic metadata; constrained inline `with` option keys, list/enum values, and `file`/`dir`/`join` helpers to exact key/value ranges; moved scoped outline selections to the same UTF-16-safe spans; and added CRLF plus non-BMP collision coverage.
- Batch 1010: Reconciled current workflow, uncertainty, system-solver, runtime-artifact, bytecode/result, and language-scope documentation with native implementations; separated compiler-time unsolved metadata from runtime numeric results; removed stale one-state-only, metadata-only percentile, and deferred-native-solver claims; and kept broad production solver limits explicit.
- Batch 1009: Preserved exact block and inline function return-expression spans through AST and `FunctionInfo`; pinned duplicate, missing, unresolved, dimension-mismatch, local-type, and function-purity diagnostics to compiler-owned function/local/expression ranges; and added CRLF plus non-BMP UTF-16 regression coverage.
- Batch 1008: Preserved exact function parameter and return type/unit spans through AST and public semantic metadata; moved signature quantity/unit colors, unknown-type diagnostics, and function/parameter/local outline selections to compiler-owned UTF-16-safe ranges; and added CRLF, repeated-name, and non-BMP signature regression coverage.
- Batch 1007: Preserved exact import-target and const name/type/unit/expression spans through public semantic metadata; pinned import/const diagnostics, namespace/readonly/type/unit semantic tokens, and outline selection ranges to compiler-owned UTF-16-safe locations; and added CRLF, repeated-name, and non-BMP path regression coverage.
- Batch 1006: Preserved exact system/component type, unit, and expression spans plus explicit operator name spans through public semantic metadata; made typed state-space vectors and linear-operator type expressions solver-colored from compiler ranges; and pinned vector type/initial-value/member and operator type/matrix diagnostics to their actual source expressions.
- Batch 1005: Promoted top-level state-space `states`/`inputs`/`outputs` type blocks and members from private solver-only metadata to a public span-preserving semantic surface; added exact role-aware tokens, structured hovers, completions, nested outline symbols, type references/rename, review JSON, VS Code fallback selectors, and TextMate member-capture coverage without same-name token overlap.
- Batch 1004: Preserved parser-owned type/unit, port-domain, object-class, and copy-source ranges through public semantic metadata; constrained structural reference highlighting to those ranges; split generic types into non-overlapping identifier tokens; and stopped unit fallback scanning from classifying declaration labels as units, with CRLF and repeated-name collision coverage.
- Batch 1003: Split domain-variable, component-port, and class-method keyword anchors from lexer-owned name spans, preserved the exact ranges in public semantic metadata, and moved their LSP declaration overlays off same-line text search with CRLF AST/semantic/token coverage.
- Batch 1002: Preserved parser-owned name spans in semantic metadata for schema columns, system variables, state vectors, component parameters/inputs/locals, class fields, args fields, and class object declarations/fields, then replaced their LSP declaration line searches with validated exact ranges and CRLF regression coverage.
- Batch 1001: Replaced the conditionally overloaded `SemanticProgram.components` field with explicit `component_templates` and `component_instances` collections, added documented `assembly_components()` and `component_symbols()` views, migrated graph/report/runtime/IDE consumers by intent, and kept both template declarations and instance bindings visible to LSP tokens, hovers, completions, references, and document symbols.
- Batch 1000: Separated schema/system/domain/component/class keyword anchors from exact declaration-name spans, preserved those parser-owned ranges in schema and semantic container metadata, reset cloned component instances to their binding-name spans, and made normal plus system review-risk LSP overlays consume the same exact ranges with CRLF compiler, instance, and snapshot coverage.
- Batch 999: Parsed function parameters from lexer token segments, preserved exact function/parameter/local name spans through `FunctionInfo`, moved their LSP declaration overlays to compiler-owned ranges, and prevented scoped reference scans from adding overlapping token types when a function and parameter repeat the same name.
- Batch 998: Separated declaration keyword anchors from exact const/system/state-vector name spans, preserved parser-owned name ranges in `TypedBinding` and `HoverHint`, and made LSP declaration semantic tokens prefer those exact ranges before the guarded line-search compatibility path, with CRLF compiler and repeated-name LSP coverage.
- Batch 997: Added persistent `eng-lsp` `$/cancelRequest` handling with reader-thread request registration, numeric/string ID isolation, standard `-32800` responses across request methods, cooperative workspace symbol/reference/rename scan interruption, completed-request cleanup, and stdio coverage proving cancellation does not poison a reused request ID.
- Batch 996: Added persistent `eng-lsp` `didChange` diagnostics debounce with a bounded stdin reader queue, configurable 0-5,000 ms initialization delay, latest-version/dependent-document coalescing, immediate open/save/close handling, and protocol coverage proving editor requests remain responsive while diagnostics wait.
- Batch 995: Restored a Rust 1.96 Clippy-clean all-targets workspace, replaced ambiguous positional compiler/runtime/LSP builders with named input contexts, simplified iterator and option handling, bounded large fixed-point enum payloads, repaired CLI item ordering, and removed stale native IDE completion/range patterns while keeping localized allowances only for the high-fanout document-symbol and run-plan serializers.
- Batch 994: Made inline and block `with` options preserve exact whole/key/value source spans, fixed top-level option splitting around nested calls, lists, objects, and quoted separators, corrected CRLF byte offsets, attached compiler-owned ranges to selected option/network/process/sampling diagnostics, and made LSP diagnostics prefer those ranges with UTF-16 conversion before legacy range inference.
- Batch 993: Replaced CI's mutating Rust format step with a failing `fmt-check` command, added Markdown final-LF/trailing-whitespace hygiene across 209 source files to `docs-check`, normalized three advanced-solver READMEs, documented the gates, and reconciled stale formatting/token-acceptance checklist entries with existing example and editor coverage.
- Batch 992: Added actual stale-process cancellation for invalidated shared VS Code snapshots and superseded saved-file checks, preserved same-revision request sharing, ignored callbacks after diagnostics disposal, exposed a clamped `englang.liveDiagnosticsDelayMs` setting, and added Electron/Node race coverage plus tooling-status visibility.
- Batch 991: Added shared semantic-role hover fallbacks for units, quantities, declared fields, TimeSeries axes/operations, side effects, external boundaries, uncertainty, and validation; preserved richer symbol hovers ahead of fallbacks; resolved composite units through UTF-16 semantic-token ranges; and added user-facing labels plus Rust, protocol, VS Code, and native payload coverage.
- Batch 990: Fixed the shared semantic-token modifier bitset by removing the unused `static` entry, capping the legend at the LSP-safe 31 modifiers, moving `temporal` from the signed bit 31 to bit 30, removing stale VS Code static selectors, and adding Rust/JavaScript/protocol capacity guards.
- Batch 989: Added persistent LSP semantic-token delta negotiation, opaque full-result IDs, bounded per-document result history, minimal integer-stream edits, unknown-result full fallback, close-time cache cleanup, and unsaved-buffer stdio reconstruction coverage.
- Batch 988: Added compiler-owned `ThermalTransmittance` metadata and flat, exponent, and parenthesized `W/m2/K` unit spellings; preserved balanced unit parentheses through normalization/source derivation; extended runtime alias conversion and LSP/TextMate coverage; and clarified the public line/doc/block comment policy.
- Batch 987: Promoted `%` and `1` into the compiler-owned unit registry, added attached/spaced percentage parsing and native `%`-to-`1` conversion, constrained quantity name hints to compatible unit candidates, distinguished bare numbers from dimensionless unit contexts in LSP/native IDE/TextMate highlighting, and rejected compiler/legacy unit catalog overlap.
- Batch 986: Promoted `Area`/`Volume` and `m2`/`m^2`/`m3`/`m^3` from TextMate-only assumptions into compiler-owned quantity and unit registries, native inference and runtime conversion coverage, generated editor metadata, and first-pass VS Code grammar tests with a generator drift guard.
- Batch 985: Hardened the native persistent `eng-lsp` document cache against delayed or retried `didOpen`/`didChange` notifications by rejecting non-increasing versions before diagnostics or dependent analysis can run, while preserving same-version saves; added unit, stdio, and `lsp-check` regression coverage.
- Batch 984: Added compiler-backed VS Code on-type formatting for structural `}` tokens with a current-line-only edit, string/comment filtering, stale/cancelled result guards, structural line-map rejection, focused provider smoke coverage, and shared registration with Format Document/Selection.
- Batch 983: Added a fake-`eng.exe` saved-file diagnostics backend smoke that verifies CLI routing, Problems ranges/codes/help/tags, review decoration refresh, actionable malformed JSON/exit/stderr failures, and stale document-version result rejection inside every focused and packaged VS Code gate.
- Batch 982: Split VS Code validation into focused `vscode-smoke` and `vscode-test` dev commands, so generated metadata and JavaScript contracts can be checked without packaging while the fuller gate also builds `eng-lsp` and audits semantic fallback coverage before the existing release/VSIX gate.
- Batch 981: Added a dedicated VS Code gutter warning icon only for compiler-proven high-risk side effects and external boundaries, while retaining the quieter line/overview markers for other review risks and clearing the icon through the existing stale-snapshot lifecycle.
- Batch 980: Added source-hash-verified VS Code fill/imputation and runtime fallback warnings from the latest native run, deduplicated coverage policy warnings when an explicit fill owns the source, made `fill missing` policy behavior explicit with compiler validation and non-preferred policy quick fixes, repaired its completion to insert a bound native interpolation output, and connected materialized fill outputs to native statistics and unit-correct HeatRate integration.
- Batch 979: Added source-hash-verified VS Code TimeSeries alignment warnings from the latest native run: partial/unavailable explicit `align` and `resample` outputs now receive compact inline and overview-ruler markers with counts and runtime reasons, while complete materialization, automatic pairwise metadata, and stale artifacts remain unmarked.
- Batch 978: Replaced metadata-only TimeSeries alignment/resampling hooks with native exact/nearest/linear materialization without extrapolation, made bound outputs usable by RMSE/statistics/reports/plots and truthful in the object store/VM steps, added compiler diagnostics and unambiguous public result fields, exposed those fields through LSP/VS Code/native IDE completion/hover/highlighting/inspectors, and converted the measured-vs-simulated workflow and golden contracts to consume the native resampled series.
- Batch 977: Unified normalized validation records across static Review JSON and live editor snapshots, fixed `validation_count` to match the emitted array, added target/phase/object result and rule-source metadata, corrected the Review table contract, and added grouped VS Code object validation pass/fail decorations with observed-value hovers.
- Batch 976: Added direct Native IDE bracket feedback at the caret: matching code and string-interpolation delimiters are highlighted together, unmatched delimiters are marked in red, string literals/comments no longer distort code-bracket pairing, and a cached sparse overlay updates at most two markers without rerendering the full highlighted source on caret movement.
- Batch 975: Added exact Native IDE line/column navigation from Ctrl+G or the clickable cursor location: the modal validates 1-based UTF-16 `line[:column]` input against the current buffer, rejects stale source, reveals distant rows and long columns, and preserves the resulting editor view per tab.
- Batch 974: Added a fixed Native IDE line-number gutter that stays vertically aligned with the editable and highlighted source layers, remains fixed during horizontal scrolling, updates immediately as lines are edited, and widens at long-file digit boundaries without shifting completion coordinates.
- Batch 973: Added source-ordered Native IDE problem navigation: F8/Shift+F8 and compact Previous/Next controls now cycle through diagnostics that match the active Problems filters, select exact UTF-16 ranges, wrap at file boundaries, reveal the active row, and refuse stale analysis.
- Batch 972: Preserved Native IDE editing context across tab switches and full UI rerenders: each tab now retains its UTF-16 selection, selection direction, and horizontal/vertical viewport, restores them without stealing focus, and clamps stale offsets after source-length changes.
- Batch 971: Added stale-safe Native IDE document breadcrumbs: the editor meta bar now derives the caret's file/symbol ancestry from compiler document-symbol ranges, lets each segment navigate through UTF-16-safe selections, and hides symbol ancestry while the checked source is stale.
- Batch 970: Made Native IDE execution use the buffers it just reviewed: toolbar and terminal Run now preflight every open workspace tab in one conflict-aware batch, persist changed EngLang/import/data buffers without rewriting unchanged files, skip unrelated external tabs, cancel before runtime on any conflict or concurrent edit, and apply runtime analysis only while the complete saved snapshot remains current.
- Batch 969: Prevented Native IDE silent data loss during file reuse, Save, Save All, close-with-save, and Run: tabs now retain their last disk baseline, reopening an already-open path preserves the buffer, writes are confined to existing workspace files, external disk changes stop the whole preflighted save batch before any file is written, and Run executes only after the same conflict-aware save succeeds.
- Batch 968: Made VS Code notice saved and closed static-import changes from Git, formatters, and other disk tools: a bounded `.eng` workspace watcher now ignores generated trees, invalidates editor snapshots, refreshes role-aware colors, and debounces dependent Problems checks according to live/file mode without running saved-file checks against dirty editors.
- Batch 967: Made already-rendered VS Code role-aware colors and review decorations follow modified static imports: an import edit or close now invalidates same-workspace editor snapshots, clears stale visible decorations, and issues one debounced semantic-token refresh instead of leaving split editors colored from an older import state.
- Batch 966: Made live diagnostics, hover, completion, semantic highlighting, symbols, and folding unsaved-aware across recursive static imports: the compiler now exposes canonical import-source overrides without changing runtime disk semantics, persistent LSP rechecks open dependents, and bounded workspace snapshot/completion endpoints let the native IDE and VS Code reject stale results whenever any participating modified buffer changes.
- Batch 965: Made go-to-definition unsaved-aware across static imports in the native IDE and VS Code: a bounded compiler workspace endpoint now receives every modified open EngLang document, resolves moved or dirty-only declarations from open text throughout recursive import chains, and rejects results when any participating buffer changes.
- Batch 964: Made workspace rename preparation, references, and rename unsaved-aware across the native IDE and VS Code: bounded compiler endpoints now receive every modified open EngLang document, resolve static imports from open text before disk (including declarations that exist only in dirty tabs), reject stale whole-request results, and let the native IDE stage verified multi-file edits atomically in memory without a save preflight.
- Batch 963: Added compiler-owned workspace symbol search to the native IDE with Ctrl+T, ranked keyboard navigation, exact UTF-16 jumps, stale-response rejection, and dirty-tab preservation; added a bounded `eng-lsp --workspace-symbols-stdin` open-document contract so native IDE and VS Code searches both prefer every modified EngLang buffer over disk while rejecting malformed, duplicate, non-EngLang, and out-of-workspace inputs/results.
- Batch 962: Removed the VS Code extension's 3,500-line JavaScript copy of compiler quick-fix rules after verifying all 60 explicit dispatch codes plus option/model repairs against `eng-lsp`; code actions now come only from the current unsaved buffer through the compiler-owned endpoint, with exact diagnostic binding and all-or-nothing current-file URI/range/overlap validation.
- Batch 961: Exposed compiler-backed linter quick fixes in the native IDE Problems panel and Ctrl+., using the current unsaved buffer, exact diagnostic matching, current-file-only URI validation, bounded UTF-16 edit validation, overlap/stale-buffer rejection, multi-choice selection, and modified-buffer review before Save.
- Batch 960: Added compiler-backed semantic rename to the native IDE with F2 and checked-token actions, strict prepare/workspace-edit parsing, dirty-buffer preflight, workspace/path/UTF-16/overlap/stale-source validation, all-or-nothing in-memory staging across affected files, unsaved changed tabs, and a toolbar Save All action.
- Batch 959: Extended native IDE References from current-file highlighting to static-import-aware workspace locations, preserving current-buffer read/write overlays, adding file/range navigation and result counts, validating every returned URI/range, and falling back to current-file results when another open EngLang tab is modified.
- Batch 958: Extended safe semantic rename across static file imports: the LSP now edits the importable declaration and every verified workspace reference, prefers open buffers, excludes unrelated same-name symbols, rejects truncated/unreadable/incomplete scans and conflicts as a whole, and makes the VS Code CLI client pass its workspace root while blocking when another EngLang buffer has unsaved changes.
- Batch 957: Extended standard Find All References from the current buffer to bounded workspace search for importable symbols, resolving each candidate's static import chain to the same declaration file, preferring open unsaved documents, excluding unrelated same-name symbols and generated directories, and passing the workspace root through the VS Code CLI client.
- Batch 956: Bumped static run plans to `eng-static-run-plan-v2` and separated execution-state wording from module maturity: the pre-execution document is `ready`, nodes are `loaded`/`declared`/`resolved`, and pending reruns are `scheduled` instead of misleading `planned`/`executed` values; tightened schema and runtime evidence accordingly.
- Batch 955: Added standard current-file Find All References across persistent LSP and VS Code, reusing compiler semantic occurrence scopes, honoring declaration inclusion, reading the unsaved buffer, rejecting stale responses, preserving UTF-16 ranges, and documenting the explicit no-cross-file-index limit.
- Batch 954: Replaced seed-only case cache hit metadata with content-addressed native CaseRunResult caching, calculation-hash and expected-result-SHA validation, output resume, cache replay, invalid-entry repair, truthful per-table hit/miss counts, `not_configured` runner/scheduler state for non-executed sample tables, and editor/module wording for the verified behavior.
- Batch 953: Promoted `eng.stats` from a planned placeholder to its existing compiler/runtime-backed TimeSeries summary implementation, added an explicit official import and end-to-end regression evidence, separated TimeSeries/statistics API ownership, aligned IDE completion wording and current docs, and replaced workflow 03's stale metadata-only smoke expectation with its native p95 propagation contract.
- Batch 952: Filled ordinary compiler-resolved expression references into role-aware semantic tokens, marked typed binding declarations explicitly, and added safe current-file semantic rename across LSP and VS Code with stale-buffer, conflict, reserved-name, member/import, and incomplete-coverage rejection.
- Batch 951: Replaced workflow 03's remaining `p95` metadata-only uncertainty linkage with native nearest-rank percentile sensitivity propagation, including unit-aware runtime artifacts and workflow regression gates.
- Batch 950: Added compiler-backed current-file document highlights to `eng-lsp`, VS Code, and the native IDE, including read/write ranges, unsaved-buffer requests, function-local scope isolation, string/comment/literal/unit exclusion, Shift+F12 and caret actions, exact UTF-16 rendering, and executable Rust/JavaScript/editor-contract coverage.
- Batch 949: Exposed native `CaseRunResult` fields and `run_case` options through compiler-owned editor metadata, added a complete native execution snippet and owner-aware option completion, colored result expressions and scheduler policies with checked-code roles, separated `case_runs` from CaseOutput/collection fallbacks in VS Code and the native IDE, and replaced stale workflow 02 `derive`/grammar evidence with executable `run_case` coverage.
- Batch 948: Implemented native sequential `apply run_case over ...` execution with typed per-case result expressions, `CaseRunResult` tables, result and run-manifest artifacts, calculation/result-hash-verified resume, collision-safe path validation, overwrite and fail/continue policies, final result collection, and workflow 02 coverage with zero external processes.
- Batch 947: Separated system solver `jacobian_sparsity` entries from the legacy `jacobian_seed` alias, gave each field an accurate status/schema contract, and replaced remaining report/docs seed wording for computed sparsity metadata.
- Batch 946: Preserved sampled and derived typed columns through native CaseTable, CaseOutput, and CaseResultCollection stages, separated case/input/collection status columns, made workflow 02 train/select/persist from the final collection, and corrected materialized table bytecode/result objects that were mislabeled as scalars.
- Batch 945: Added compiler-backed native IDE go-to-definition for the current unsaved buffer with F12, Ctrl+click, and checked-token actions; current-file, imported, and stdlib targets use exact UTF-16 ranges, token-start carets resolve correctly, and already-open dirty tabs are preserved.
- Batch 944: Added a compiler-backed native IDE Outline with unsaved-buffer document symbols, hierarchy and kind colors, name/kind/detail filtering, exact UTF-16 source navigation, Ctrl+Shift+O and Enter keyboard flow, and Rust/JavaScript contract coverage.
- Batch 943: Added native IDE current-file search with toolbar and Ctrl+F entry, selected-text prefill, wraparound Enter/Shift+Enter and F3 navigation, case matching, live match counts, completion-popup dismissal, and executable range/shortcut/navigation coverage.
- Batch 942: Colored builtin calls in args defaults, missing-policy options before their equals signs, and supported compound derivative units such as `K/s`, with actual TextMate role assertions across all examples.
- Batch 941: Fixed `interpolate` first-paint coloring inside schema missing-policy rows and added an actual VS Code tokenizer audit across every example keyword occurrence and both bundled themes.
- Batch 940: Protected native IDE edits with Ctrl+S, Save/Discard/Cancel for dirty tabs, native Tauri window-close interception with Save All/Discard All/Cancel, a browser unload fallback, and an executable editor safety smoke.
- Batch 939: Isolated shared VS Code live-analysis work from individual provider cancellation, sequenced Problems checks per document so stale same-version results cannot overwrite current diagnostics, and added an executable race smoke.
- Batch 938: Repaired dev-current packaging after the docs/tutorials removal by copying current user and workflow Markdown with source-relative structure, excluding documentation build scripts, and making portable IDE smoke use compile-time UI contracts instead of repository source paths.
- Batch 937: Added debounced native IDE diagnostics and role-aware color refresh while typing, rejected stale async check/run results, refreshed Problems and highlight views without rebuilding the editor, and fixed visible status and line-count updates.
- Batch 936: Moved builtin editor role groups into compiler-owned syntax metadata, wired native IDE first-paint calls and generated TextMate scopes to that catalog, and restricted percentile highlighting to implemented `p90`/`p95` helpers.
- Batch 935: Removed the unsupported `datetime(...)` editor seed and aligned supported external, path, and `date(...)` helpers across LSP semantic modifiers, TextMate first-paint scopes, VS Code themes, native IDE colors, and regression tests.
- Batch 934: Split `fill_missing(...)` and compatibility-only `select_first_row(...)` out of generic TextMate builtin coloring into validation and deprecated helper scopes, including dark/light theme colors, semantic fallback mappings, and grammar regression fixtures.
- Batch 933: Replaced the misleading `fill_missing` editor completion with canonical `check coverage` and `fill missing` workflow snippets, so completion inserts public EngLang syntax and its explicit policy blocks instead of an internal strategy spelling.
- Batch 932: Removed the compatibility-only CaseOutput planned_count member alias from compiler/runtime member lookup and guarded the public API on expected_count.
- Batch 931: Split workflow docs between the implemented native materialize/apply/collect case-table path and the remaining general run-case scheduler work, and renamed internal case-manifest seed test labels to record labels.
- Batch 930: Made VS Code Tooling Status native workflow summaries explicitly say no external processes alongside process_count=0 so zero-process evidence is readable without interpreting raw counters.
- Batch 929: Renamed workflow case-table artifact runner status from manifest_seed_runner to native_template_runner and added smoke/runtime guards so native case execution no longer reads as seed-only.
- Batch 928: Added native primitive evidence summaries to workflow-native-status and VS Code Tooling Status so workflow 01/02/03 show concrete native API, sampling/model/DB, and uncertainty/report primitives instead of only no-Python checks.
- Batch 927: Mirrored domain helper TextMate scopes inside string interpolation so model, uncertainty, TimeSeries, and solver helper calls color before the generic builtin fallback.
- Batch 926: Colored parent(...), stem(...), and extension(...) as path helper TextMate calls alongside join(...), including interpolation coverage, grammar smoke, and token-scope docs.
- Batch 925: Colored exists(...) as an external-boundary TextMate helper instead of the last generic builtin first-paint helper, with grammar smoke and token-scope docs updated.
- Batch 924: Reworded VS Code README quick-fix merge and cache invalidation text away from fallback-repair/state wording toward local quick fixes and cached review/highlight data, with a contract guard.
- Batch 923: Reworded VS Code README model source-chain quick-fix coverage from skeleton repairs to starter-code repairs and guarded against skeleton-repair wording returning.
- Batch 922: Added theme fallback scope summary aliases to VS Code highlight inspector and Tooling Status probe payloads, keeping legacy fallback_scope_status fields for compatibility.
- Batch 921: Reworded internal_planned stdlib module labels/details from Internal planned and target-surface wording to Internal target and explicit public stdlib API boundaries, then regenerated VS Code editor metadata.
- Batch 920: Added theme_coverage_status and theme_fallback_scope_count to VS Code highlight inspector/copy payloads while keeping fallback_status compatibility fields, with README and contract coverage.
- Batch 919: Reworded VS Code planned/internal stdlib import hover text away from workflow-surface/internal-boundary wording toward explicit public stdlib API status, with an ide-check guard.
- Batch 918: Reworded VS Code highlight-inspector README coverage wording from mapped/missing fallback status to theme fallback coverage state, with an ide-check guard.
- Batch 917: Reworded current docs away from raw JSON/source-diff phrasing toward manual JSON artifact and line-by-line source comparison wording, with docs-check guards.
- Batch 916: Reworded the native IDE how-to Tooling Status summary from the old fallback-status phrase to configured-path/source status and guarded user docs against the old phrase.
- Batch 915: Reworded VS Code Tooling Status configured executable path states away from fallback wording toward explicit discovered-tool labels and guarded against the old fallback phrasing returning.
- Batch 914: Renamed VS Code highlight inspector advanced payload fields from `raw` to `advanced_highlight_data` and guarded against the raw payload label returning.
- Batch 913: Added local VSIX package/install freshness and install preflight guidance to VS Code Tooling Status when the active workspace is an EngLang source checkout.
- Batch 912: Split the remaining workflow-step, solver, and path helper TextMate scopes so `apply(...)`, `run_case`, `der(...)`, and `join(...)` no longer use generic builtin first-pass colors.
- Batch 911: Added an external-boundary helper TextMate scope so `file(...)`, `url(...)`, `env(...)`, and `secret env(...)` use external-boundary first-pass colors instead of generic builtin colors.
- Batch 910: Added a TimeSeries/statistic helper TextMate scope so `integrate(...)`, `mean(...)`, `time_weighted_mean`, and `p90` use TimeSeries-specific first-pass colors instead of generic builtin colors.
- Batch 909: Added an uncertainty helper function TextMate scope so `measured(...)`, `uniform(...)`, `propagate(...)`, and `probability(...)` use uncertainty-specific first-pass colors instead of generic builtin colors.
- Batch 908: Added a model helper function TextMate scope so `train_test_split(...)`, `regression(...)`, `evaluate(...)`, and `model_card(...)` use model-specific first-pass colors instead of generic builtin colors.
- Batch 907: Colored model workflow phrase keywords such as `predict ... using ...` and `train regression ...` with the model TextMate scope so first-pass VS Code colors match LSP model roles sooner.
- Batch 906: Added TextMate first-paint coverage for class object construction and copy-with headers so object source names and `with` use model-colored scopes before semantic tokens arrive.
# Usability Improvement Backlog

- Batch 905: Added TextMate first-paint coverage for `rmse measured.T vs simulated.T` comparison phrases so the validation metric, `vs` operator, and dotted measured/simulated operands are colored consistently before semantic tokens arrive.
- Batch 904: Extended native workflow guards so workflow 01/02/03 audit every support-file path plus README/text docs for Python, `.py`, notebook, and run-command regressions instead of only checking executable `.eng` sources.
- Batch 903: Added an LSP regression guard that scans every `examples/**/*.eng` source and fails if any keyword semantic token falls back to an empty modifier set, keeping role-aware keyword coloring from regressing.
- Batch 902: Marked the domain conservation `is shared at connected ports` connector as solver-colored and verified all example keyword semantic tokens now carry a role modifier instead of falling back to generic keyword colors.
- Batch 901: Marked class/object copy-with headers so `source with {` keeps the source object and `with` in the model semantic color family, including invalid in-progress copy expressions before semantic object info is available.
- Batch 900: Colored measured-vs-simulated RMSE comparison expressions so `rmse measured.T vs simulated.T` marks `vs` and both dotted operands with report/timeseries/validation semantic modifiers instead of generic keyword/property colors.
- Batch 899: Marked statement-leading `return` keywords with the local semantic modifier so function return lines use role-aware editor coloring without repainting catalog literals named `return`.
- Batch 898: Marked validation/filter condition words such as `is`, `none`, `or`, and range `and` with validation semantic modifiers so schema constraints and `where` filters no longer leave boolean condition syntax as generic keyword colors.
- Batch 897: Marked `use` and `import` keywords with declaration/imported semantic modifiers so file and namespace imports use the same editor color family as imported namespace tokens.
- Batch 896: Marked contextual `linear` constant literals with uncertainty/model semantic modifiers so propagation methods, regression algorithms, and uncertainty options do not fall back to generic keyword colors.
- Batch 895: Marked equation `eq` and TimeSeries `of` operator words with solver/timeseries semantic modifiers so checked-code highlighting no longer leaves equation and series type clauses as generic keywords.
- Batch 894: Marked declaration keywords such as `schema`, `domain`, `component`, `system`, `class`, `fn`, `method`, `package`, and `version` with declaration semantic modifiers so checked-code highlighting preserves declaration colors instead of generic keyword colors.
- Batch 893: Added role-aware fallback coloring for partial workflow/report clause keywords so `sort ... by`, `join ... with`, `summarize ... by`, repeated plot `and`, `show ... as`, and summary field `as/with` no longer fall back to generic keyword colors.
- Batch 892: Colored `connect ... to ...` solver endpoints and `to` clauses in LSP fallback highlighting so component connection lines no longer drop to generic keyword/property colors before richer semantic context.
- Batch 891: Made partial DB table read/write targets role-aware in LSP fallback highlighting so `write ... to args.database.table(...)`, `read sqlite ... as ...`, and copy/move `to` clauses keep DB/external colors before full type resolution.
- Batch 890: Marked export-summary field connectors as report-aware semantic tokens so `as` and `with` in summary CSV field rows use report role colors instead of generic keyword coloring.
- Batch 889: Clarified workflow 02 case status wording by separating initial CaseTable manifest state from final CaseResultCollection status in bindings, prints, and docs.
- Batch 888: Made VS Code saved-file uncertainty argument diagnostics underline invalid named argument values such as `samples=many` instead of the uncertainty constructor name.
- Batch 887: Routed VS Code saved-file `E-WITH-UNIT-001` diagnostics through option-value fallback ranges so incompatible report display units underline the requested unit value.
- Batch 886: Made VS Code saved-file Problems fallback ranges parse print/write interpolation fields so format unit diagnostics underline the requested unit instead of the expression binding.
- Batch 885: Added workflow-aware semantic modifiers to network, process, report, and solver with blocks so their options and helper values stay visible in workflow highlight filters.
- Batch 884: Made VS Code and native IDE highlight coverage phrase-aware so split role-aware tokens still cover multi-word workflow options such as `unit x` and `unit y`.
- Batch 883: Marked model workflow `with` blocks as workflow-aware semantic keyword ranges so the block keyword matches the model options it contains.
- Batch 882: Fixed CSV promotion member completion so schema columns remain reachable when generic table fields do not match the typed member prefix.
- Batch 881: Promoted model workflow connectors (`from`, `on`, `using`) and model with-block options to workflow-aware highlighting in TextMate and LSP semantic tokens.
- Batch 880: Set the VS Code and native IDE Public fields highlight coverage filter to `property`, matching the semantic token selectors users actually filter in highlight tables.
- Batch 879: Aligned native IDE Highlight coverage inspection with VS Code by adding Types, Quantities, and Public fields from the shared editor metadata catalog.
- Batch 878: Expanded VS Code highlight coverage inspection to report Types, Quantities, and Public fields from shared editor metadata, so color gaps outside keyword/unit scopes are easier to see.
- Batch 877: Made `workflow-native-status` explicitly report no Python/.py/notebook/run-command markers, no external processes, and no process/Python run-graph nodes for workflow 01/02/03 evidence.
- Batch 876: Colored `write text`, `write json`, and `write standard_text` format selectors as side-effect first-paint tokens and added grammar guard coverage for those phrase captures.
- Batch 875: Added `standard_text` to shared editor workflow keyword metadata so VS Code and native IDE keyword catalogs expose the native standard-file writer consistently.
- Batch 874: Split VS Code first-paint theme colors for function declarations, calls, built-in helpers, member fields, and public members so code is more readable before role-aware semantic colors arrive.
- Batch 873: Split base VS Code EngLang theme colors for functions, methods, and properties so generic calls and member fields remain visually distinct before domain-specific role colors apply.
- Batch 872: Removed the remaining raw internal metadata wording from the native IDE how-to and guarded against that phrase returning in user-facing IDE docs.
- Batch 871: Reworded LSP/VS Code read text/json/toml completion details from raw read wording to direct read wording, regenerated editor metadata, and added guards against the older completion details returning.
- Batch 870: Reworded the native IDE highlight detail expander from raw JSON wording to Advanced highlight data, renamed the shared expander class/helper accordingly, and added IDE guards against the older raw label returning.
- Batch 869: Reworded the portable user guide away from raw artifact-file wording toward explicit JSON artifact files, with a user-docs guard against the older phrase returning.
- Batch 868: Reworded workflow 01 API docs and printed output away from generic payload wording toward native HTTP response and API JSON contract language, with docs guards against the older phrases returning.
- Batch 867: Archived current backlog batches 841-856 into the historical log so active API/IDE/workflow cleanup docs stay focused on the latest linter, highlighting, and native IDE usability work.
- Batch 866: Added native IDE Copy at cursor for Problems plus source-line diagnostic copy text so linter reports can be captured from the caret without reconstructing context manually.
- Batch 865: Reworded VS Code README copy/inspect guidance from implementation-detail copy wording to copy-ready diagnostic/highlight details and advanced highlight data.
- Batch 864: Added role-aware highlighting aliases to VS Code highlight inspector payloads and reworded remaining highlight warnings/status summaries away from semantic-highlighting implementation terms.
- Batch 863: Added native IDE status-label mappings for behavior graph not-connected solver states so advanced solver panels avoid leaking raw `*_not_integrated` artifact keys.
- Batch 862: Reworded VS Code Tooling Status and current docs away from implementation-detail highlighting internals toward checked-code role-aware color wording, with a package guard against the older phrasing returning.
- Batch 861: Moved the native IDE Highlight tab into the primary review flow immediately after Review, with side-tab order guards and user-doc wording so color/range debugging is easier to find.
- Batch 860: Color-coded native IDE Highlight panel chips for token categories, details, selectors, and coverage domains so the inspector mirrors the editor's role colors instead of showing mostly generic chips.
- Batch 859: Reworded VS Code and native IDE user-facing highlighting settings/docs from implementation-detail phrasing to checked-code and role-aware wording, with guards against the older wording returning.
- Batch 858: Split VS Code EngLang theme colors across unit, quantity, TimeSeries, workflow, validation, report, side-effect, external, solver, model, DB, and cache role families so role-aware highlighting is more colorful without collapsing each family into one color.
- Batch 857: Reworded VS Code highlighting docs away from TextMate/semantic implementation phrasing toward first-pass syntax colors and role-aware colors, with a guard against the older wording returning.

This file is the short current backlog for API clarity, native workflow usability,
editor/linter behavior, and documentation cleanup. The historical batch log was
archived to [usability_improvement_backlog_history.md](../archive/usability_improvement_backlog_history.md).

## Current Priorities

1. Keep workflow 01/02/03 native-only: no Python, `.py`, or `run command` path may re-enter those examples.
2. Replace remaining metadata-only or synthetic implementation paths with executable compiler/runtime behavior where the public docs imply support.
3. Improve VS Code and native IDE authoring quality: consistent TextMate first-paint highlighting, checked-code role-aware colors, precise diagnostics, hover, completion, and quick fixes.
4. Reduce API wording ambiguity in public examples, generated metadata, diagnostics, and command names.
5. Keep current docs compact and task-oriented; move implementation history and long-form plans to `docs/archive` or `docs/internal`.

## Open Candidates

- Cache replay and invalidation: network offline-response replay, native case-result cache replay/repair, and `eng cache invalidate` manifest-path deletion are implemented with hash/path safety checks; broader process/model replay, shared/remote case caches, and cross-artifact invalidation design remain open.
- Live network execution: live HTTP(S) GET/download and POST/PUT/PATCH string request bodies are implemented with timeout, retry, body limits, SHA-256 verification, body hashes, cache replay, and redacted Secret query/header records; broader live secret injection and auth schemes still need a public contract.
- Model training surface: native `train regression <table>` feeds model-card, metrics, and prediction-table paths; broader algorithm coverage and clearer multi-model naming remain open.
- Case orchestration: native `materialize cases`, template `apply`, sequential
  or bounded parallel `apply run_case`, and `collect results` now materialize
  CaseTable/CaseOutput/CaseRunResult/CaseResultCollection records and per-case
  result manifests. Native results now use a calculation-hash/result-SHA
  verified local cache with replay and invalid-entry repair. Automatic
  external-adapter dispatch and shared/remote cache adapters remain open.
- DB query support: typed SQLite table readback is implemented; arbitrary query APIs, parameter binding, and query transaction policy remain open.
- Native IDE usability: current-file Outline, workspace symbol search, unsaved-aware static-import definition/references/rename, and compiler quick fixes are implemented; keep improving token insight and inspector flows for repeated debugging tasks.
- VS Code navigation: current-file highlights plus unsaved-aware workspace symbols and static-import definition/references/rename are implemented; broader package/index identities remain open.
- VS Code linter/highlighting: `where`/`with` block anchors, `where` locals, `with` option keys/values, `write` sources, fast-binding RHS values, promotion bindings/sources/schema targets, command targets/clauses, assertion operands/operators, sampling declarations/distributions, class defaults/validations/method signatures/object fields, domain generics/variables, component parameters/equations/behavior calls, connect endpoints, physical equation sides, ML source/input/argument operands, uncertainty constructor arguments/sources, import namespaces, and `apply` callables now have first-class parser or compiler-derived ranges. Imports remain atomic namespace tokens; dotted promotion/ML/uncertainty/apply paths are emitted as non-overlapping parameter, receiver, property, and function segments; and lexical strings are split around interpolation, format, unit, and quoted-namespace roles. Diagnostics across the 153-file example, diagnostic, and grammar-fixture corpus use compiler-owned declaration, expression, domain, endpoint, operand, key, value, function, or argument spans. The coverage gates reject every overlapping semantic-token pair and every diagnostic that would require LSP text inference; the current fallback count is zero.

## Documentation Policy

- Public behavior changes update user docs, reference docs, examples, and release notes when applicable.
- Runtime artifact changes update the artifact reference and schemas when their shape changes.
- Historical implementation logs belong in `docs/archive`; internal unstable design work belongs in `docs/internal` or focused `docs/current/*_plan.md` files.
- `docs/README.md` remains the navigation entry point; avoid adding parallel indexes unless they serve a specific reader path.

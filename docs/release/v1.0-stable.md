# v1.0-stable Release Notes

Status: ready as stable core release.

## Scope

v1.0-stable closes the first execution arc:

```text
typed CSV boundary -> unit-aware calculations -> TimeSeries statistics -> PlotSpec/report -> minimal system metadata with explicit solver boundary -> packaged runner
```

Included:

```text
- workspace version bumped to 1.0.0
- official units, CSV/report, CSV+plot, and simple system examples pass
- stable `eng run` artifact path through bytecode v1 and result.engres v1
- row-level runtime table pages for the official CSV example
- computed mean/max/p95/duration_above statistics and trapezoidal HeatRate
  integration for the official coil TimeSeries path
- schema constraint and missing policy execution status for the official CSV path
- numeric missing value interpolation for runtime table pages
- strict/inclusive numeric upper/lower bound constraint execution for runtime table pages
- review.json, report_spec.json, report.html, PlotSpec, SVG, and plot manifest generation
- CSV-derived PlotSpec points with title, y-axis unit, and plot type options applied
- line, bar, and histogram SVG rendering seeds for PlotSpec points
- minimal system/equation residual metadata, system IR dependencies, and explicit
  unsolved solver boundary
- portable zip package and SHA256 checksum
- portable package smoke under a path containing spaces and Korean characters
- runnable standalone bundle from `eng build --standalone`
- standalone run.bat execution that produces report and PlotSpec artifacts
- `.engpkg` package metadata promoted to `engpkg-stable-1`
- standalone lock file records runtime/compiler/bytecode/result/report/plot versions
- Args defaults and CLI-provided values are recorded as arg_values
- promote csv args.input resolves through default or CLI-provided Args values
```

## Commands

```bat
.\dev.bat setup
.\dev.bat doctor
.\dev.bat ci
.\dev.bat package
.\dev.bat package-smoke
.\dev.bat release-check
```

Standalone bundle:

```bat
target\debug\eng.exe build examples\official\01_csv_plot\main.eng --entry main --standalone --profile repro
dist\main-standalone\run.bat
```

Portable package:

```text
dist\englang-preview\
dist\englang-preview-v1.0.0-windows-x64.zip
dist\englang-preview-v1.0.0-windows-x64.zip.sha256
```

Standalone bundle:

```text
dist\main-standalone\
  eng.exe
  run.bat
  main.engbc
  main.engpkg
  main.lock
  main.review.html
  source/
    main.eng
    data/sensor.csv
```

## Verified Behavior

`.\dev.bat ci` verifies:

```text
- compiler/report/runtime unit tests
- official examples pass
- expected error examples produce diagnostics
- CSV+plot example produces report and PlotSpec artifacts
- artifact golden checks validate CSV runtime columns, computed statistics,
  integration value, policy execution results, and real PlotSpec points
- data quality fixtures validate parse failures, numeric interpolation, and
  row-level constraint violations
- simple system example produces system summary, system IR, derivative-state,
  dependency, and solver-boundary artifacts
- Korean and space-containing source/build path smoke
- standalone packaged runner smoke
- clippy with warnings denied
```

`.\dev.bat package-smoke` verifies:

```text
- release eng.exe builds
- portable zip and SHA256 files are written
- zip extracts under a path containing spaces and Korean characters
- packaged eng.exe doctor passes
- packaged CSV+plot example creates report and PlotSpec artifacts
- packaged simple system example creates report artifacts
- packaged eng.exe builds a standalone bundle
- standalone bundle run.bat creates report and PlotSpec artifacts
```

## Stable Boundaries

Stable for v1.0:

```text
- v8/v9 fast `=` syntax policy
- typed CSV promote validation
- entry-based run/build policy
- bytecode v1 text artifact
- result.engres v1 typed payload
- review.json/report_spec.json/report.html review path
- PlotSpec v1 and native SVG plot output
- minimal system/equation residual metadata, system IR dependency metadata, and
  explicit unsolved solver boundary
- portable package and packaged runner execution
```

Important qualification:

```text
v1.0 is stable as an artifact-contract and packaged-runner release. It does not
claim a general-purpose expression VM for arbitrary table formulas, generated
typed Args conversion beyond string-valued flags, or numeric system simulation.
```

Deferred:

```text
- numeric ODE/system solver
- broad table expression execution beyond the supported official CSV coil path
- time-weighted mean and broader statistics kernels
- full histogram binning and grouped/stacked bar plot semantics
- rich typed conversion for non-string Args-derived flags
- component/connect graph
- uncertainty propagation
- ML package
- LSP/VS Code extension
- optimized AOT standalone executable
- package registry and domain package ecosystem
```

## Next Milestone

v1.1:

```text
- Measured[T]
- Interval[T]
- Distribution[T] seed
- Ensemble[T] seed
- uncertainty metadata
- simple propagation
- uncertainty report summaries
```

## Release Workflow

v1.0-stable was tagged before the GitHub release workflow existed. To publish
or refresh the GitHub Release assets after this workflow is merged, manually run
`.github/workflows/release.yml` with:

```text
tag_name = v1.0-stable
```

For the public SemVer GitHub Release, use tag `v1.0.0`. The release workflow
falls back to this file for `v1.0.0` release notes.

Post-release hardening is tracked in
[v1.0 gap audit](../development/05_v1_0_gap_audit.md).

NATIVE-SURROGATE-CASE

This file is rendered by applying the native workflow template step over a
materialized case row. Domain-specific formats such as IDF, FMU, solver decks,
or laboratory equipment recipes can be generated by adapters above this same
typed case boundary.

case={{case_id}}
annual_electricity={{annual_electricity: kWh}}
peak_cooling={{peak_cooling: kW}}

# B01 CSV Heat Rate

Focus: CSV promotion, TimeSeries arithmetic, and trapezoidal integration.

Run:

```bat
eng.exe jit-bench benchmarks\B01_csv_heat_rate\main.eng --iterations 1
```

Expected coverage is recorded in `expected.json` and checked by
`dev.bat jit-check`.
